import { redirect } from "next/navigation";

/** Public veterans hub removed — content lives in admin (VA Path, VA Loans, Business Plan). */
export default function VeteransPage() {
  redirect("/");
}
